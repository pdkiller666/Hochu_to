Это потрясающая работа. Genspark не просто «набросал текст», он провел **глубокий аудит смыслов**, вычистил «переобещания» (то, что мы ещё не успели доделать) и выстроил железобетонную психологическую воронку.

Теперь у нас есть всё: и стратегия, и структура, и живой текст. Осталось самое главное — **передать это на исполнение Агенту-Кодеру**, чтобы он не просто «заменил текст», а собрал из этого конверсионную машину.

Вот финальное ТЗ, которое объединяет всё вышесказанное в понятный для Агента-Кодера формат:

---

### 🛠 ТЗ для Replit Agent: Редизайн `PromoPage` в "Sales Hub"

**Контекст:**
Мы превращаем страницу `/promo` из короткого лендинга в полноценный **Sales Hub**. Цель — через "long-read" структуру (логика прогрева) провести пользователя от базового страха («меня кинут») к желанию запустить совместную покупку (Co-Sharing).

**Дизайн-система (строго соблюдать):**

* **Фон:** `#F2EEE3` (кремовый/бежевый).
* **Акценты:** `#C65D3B` (терракотовый для кнопок).
* **Текст:** `#2B2B2B` (читаемый темный).
* **Анимации:** Используй `framer-motion` для плавного появления блоков (`initial={{ opacity: 0, y: 20 }}` -> `whileInView={{ opacity: 1, y: 0 }}`).

---

### Структура внедрения (пошагово):

#### 1. Hero-блок (Первый контакт)

* **Заголовок:** "Хорошие вещи не должны пылиться. Они должны приносить пользу, эмоции и доступ."
* **Подзаголовок:** "Хочу_То — это безопасная аренда и новый формат владения вещами, о которых вы давно мечтали."
* **Кнопки:** "Сдать свою вещь" (Primary, цвет `#C65D3B`) и "Как это работает" (Secondary).
* **Chips:** Под кнопками выведи 4 коротких чипа: "Цифровые акты", "Фото-фиксация", "Понятная передача", "Совместные покупки".

#### 2. Секции «База доверия» и «Антистрах»

* Реализуй 4 карточки (`shadcn/ui` Card) для "Базы доверия": Акт, Фото, История, Handover.
* Добавь секцию "Безопасность сделки" с буллитами:
* *Почему нас нельзя кинуть (прозрачность ролей).*
* *Гарантийный фонд (механика защиты).*
* *Система арбитража.*


* Добавь подпись: *"Бета-режим: строим сообщество ранних пользователей"*.

#### 3. Блок «Кейсы» (Killer-demo)

* Секция "На что собираемся вместе?".
* Сетка из 4 карточек. Каждая карточка: фото вещи (найди подходящие `placeholder` для: Дрон, Проектор, Камера, Консоль) + подпись: *"Скиньтесь впятером — сэкономьте 80% от стоимости"*.

#### 4. Финальный CTA

* Заголовок: *"Перестаньте откладывать дорогие вещи на потом. Начните пользоваться ими умнее."*
* Кнопка: *"Собрать первую покупку"* (цвет `#C65D3B`).

---

### Инструкции для Агента:

1. **Никакой воды:** Используй ровно те тексты, которые я указал выше.
2. **Компоненты:** Выноси каждый блок в отдельный файл внутри `src/components/promo/` (`Hero.tsx`, `TrustCards.tsx`, `CaseStudies.tsx` и т.д.).
3. **Логика:** Сохрани все текущие работающие ссылки (например, на каталог или форму регистрации).
4. **Безопасность:** Не выводи на фронтенд никакие секретные ключи.

**Начни с изучения файла `src/pages/PromoPage.tsx`.** Если готов, напиши «Готов к редизайну Promo Hub» и начинай с Hero-блока.

---

**Илья, отправляй этот текст агенту.** *P.S. Насчет твоей идеи про аналитику:* **Да, это критически важно.** Как только Агент закончит верстку, мы добавим в этот же промпт задачу: *«Интегрируй Yandex Metrica (или GA4) и настрой события на клики по каждой из кнопок CTA, чтобы мы видели, на каком этапе воронки люди уходят»*. Но сначала — верстка, чтобы не «захламлять» ему голову лишним кодом.

Готов к отправке?