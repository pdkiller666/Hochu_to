Правильный, по-настоящему управленческий подход. Мы не переходим к новым фичам (типа аналитики), пока у нас есть критический баг (Critical Blocker) на стороне пользователя. Инфографика — наша киллер-фича, она должна работать идеально.

Как только твой новый Replit-Агент проведет Health Check, клонирует репозиторий и напишет, что готов к работе, отправляй ему этот промт. Мы заставим его исправить стили SVG-файла так, чтобы они опирались на системные шрифты, которые гарантированно есть в Linux-контейнерах Amvera.

***

### 📝 Промт для Replit-Агента: Stage 30B-Fix

> **Task: Stage 30B-Fix — Fix Cyrillic Font Rendering (Tofu Bug) in AI Infographic**
> 
> **Context:** > We successfully deployed to production (Amvera), which uses a minimal Linux container (`node:20-slim`). However, the `sharp` library generates squares ("tofu") instead of Cyrillic text when generating the Infographic. This happens because the container lacks specific fonts (like Arial) and fails to fallback correctly for Cyrillic characters.
> 
> **Action Required:**
> 1. Open `lib/image-service.ts` (where `buildInfographicImage` and the SVG template are defined).
> 2. Locate the `<svg>` string generation.
> 3. **Critical:** Change the `font-family` for all `<text>` elements. Remove any hardcoded Windows/Mac fonts. Set it strictly to a generic fallback stack optimized for minimal Linux containers:
>    `font-family="system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Ubuntu, 'Helvetica Neue', sans-serif"`
> 4. Ensure there is no trailing CSS that forces a non-existent font.
> 5. Review the SVG `<text>` elements to ensure they don't break if the font width varies slightly (use reasonable spacing and `text-anchor`).
> 
> **Commit:**
> Push the fix via `bash scripts/github-push.sh "fix: update infographic SVG font-family to support Cyrillic on Amvera"`.

***

**Что произойдет дальше:**
Агент поправит генератор картинок и отправит код на GitHub. Amvera автоматически пересоберет платформу. 
Как только деплой закончится, просто нажми кнопку «🪄 Создать инфографику» на любом объявлении еще раз. 

Жду отчет! Если вдруг квадратики останутся (что бывает крайне редко на `node-slim`), у меня есть запасной "тяжелый" план — мы заставим Агента написать скрипт принудительной установки русских шрифтов прямо в ядро сервера. Но начнем с легкого и безопасного пути!