Глобальная зачистка «Бета-режима» (Bugfix)  Нам нужно, чтобы Агент прошелся по всему фронтенду (карточки вещей, создание объявления, чекаут бронирования) и спрятал всю коммерцию, если рубильник выключен.  Скопируй и отправь ему этот промт:  Task: Global UI/UX Sweep for Beta Mode (is_commercial_mode === false)  We successfully implemented the Beta mode constraints for the Co-sharing pools, but we found a bug: our standard Listings (Rentals) are still showing commercial features (Guarantee Fund, Platform Fees, Payment gateways) when isCommercialMode is false.  We need to do a comprehensive UI/UX sweep to ensure the Free Beta experience is strictly enforced across the app.  1. Global Settings Context:  Ensure you are using the isCommercialMode value from the platform settings context/helper across the frontend.  2. Listing Creation & Editing (CreateListing.tsx / EditListing.tsx):  If isCommercialMode === false, hide or disable the "Гарантийный фонд" (Protection Enabled) toggle. Force it to false in the backend submission if necessary. Explain with a small tooltip/text: "В бета-версии защита платформы отключена. Сделки проводятся на доверии."  3. Listing Details & Cards (ListingDetail.tsx, ListingCard.tsx):  Hide any badges or text mentioning "Защищено платформой" or "Гарантийный фонд" if commercial mode is false.  4. Booking Checkout Flow (BookingModal.tsx or similar pricing display):  If isCommercialMode === false:  Hide the "Сервисный сбор платформы" (Platform Fee) line item in the price breakdown (it should be 0 anyway, but hide the UI row).  Hide the "Взнос в гарантийный фонд" line item.  Change the primary call-to-action button from "Оплатить" (Pay) to "Забронировать" (Book) or "Отправить заявку" (Send Request).  Add a prominent Info Banner: "Бета-режим: Оплата аренды происходит напрямую владельцу при встрече (наличными или по СБП)."  5. Backend Verification (routes/bookings.ts & lib/pricing.ts):  Double-check that if platform_settings.is_commercial_mode is false, the backend pricing calculation strictly sets service_fee and protection_fee to 0, even if a user somehow bypasses the frontend.  Please find all instances where commercial UI elements leak into the Beta experience and wrap them in {isCommercialMode && ...} constraints. Push via github-push.sh "Bugfix: Global UI sweep to enforce free Beta Mode across listings and bookings".  Этот промт заставит Агента вычистить все несостыковки. Платформа станет выглядеть как уютный, бесплатный клуб для своих, что идеально для старта и сбора первых отзывов