Task: Stage 33.1.5 - Safe Technical Debt Polish (Buyout UI & DB Snapshot)CRITICAL CONTEXT & SAFETY RULES:
The production deployment on Amvera is currently STABLE.
DO NOT modify package.json, vite.config.ts, Dockerfile, or any backend routes/services (artifacts/api-server/**).
This is a strictly isolated frontend UI polish and a local DB script update.  Action 1: Fix dev-data.sql snapshot (Local DB read-only)
The current scripts/db-snapshots/dev-data.sql is outdated and incompatible with the current schema.  Run the pg_dump command exactly as specified in the docs (Section 5a) to refresh the snapshot using the current local dev database:
PGSSLMODE=require pg_dump "$DATABASE_URL" --data-only --column-inserts --no-owner --no-privileges -t users -t regions -t categories -t listings -t bookings -t booking_events -t booking_messages -t reviews -t notifications -t platform_settings -t support_tickets -t support_messages -t claims -t contact_balances -t contact_purchases -t joint_purchases -t newsletter -t admin_audit_log -t favorites -t reports -t pools -t pool_shares -t share_offers -t digital_acts -t payments -t listing_promotions -t listing_views -t audit_events > scripts/db-snapshots/dev-data.sql
(Note: ensure you include the new tables like pools, pool_shares, digital_acts, payments, etc.)  Action 2: Refactor BuyoutBlock.tsx (Frontend ONLY)Open artifacts/hochu-to/src/pages/PoolDetail.tsx (or where BuyoutBlock is located).  Find any instances of native window.confirm() used for buyout actions (e.g., canceling a request).  Replace them with the AlertDialog component from @radix-ui/react-alert-dialog (matching the style we used in ListingForm.tsx during Stage 33.0).  Ensure there is a clear "Отказаться" (Cancel) flow for a buyout participant, as discussed in the Stage 28 followup.  Action 3: Update Docs
Move these items from the "Technical Debt" section to completed in replit.md and add a brief log in AGENT_INSTRUCTIONS.md.  Commit:chore(db): refresh dev-data.sql snapshot; refactor(ui): replace native confirm in buyout block