meta:
  environment: node
  toolchain:
    name: pnpm
    version: 9.15.9

build:
  command: pnpm install && pnpm build

run:
  command: pnpm --filter @workspace/api-server start
  persistence:
    - mountPath: /data
      name: data