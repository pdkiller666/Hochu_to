meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  additionalCommands: "npm install -g pnpm && pnpm install && pnpm run build"
  artifacts:
    "*": "dist"
run:
  command: "npx serve -s dist"