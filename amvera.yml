meta:
  environment: nodejs
  toolchain:
    name: npm
    version: "20"
build:
  # Мы явно ставим pnpm версии 9
  additionalCommands: "npm install -g pnpm@9 && pnpm install --no-frozen-lockfile && pnpm run build"
  artifacts:
    "dist": "dist"
run:
  command: "npx serve -s dist"