meta:
  environment: node
  toolchain:
    name: npm
    version: 20

build:
  # Оставляем пустым. Amvera по умолчанию выполнит npm install и npm run build

run:
  # scriptName — это стандартное поле, оно должно пройти
  scriptName: start
  persistence:
    - mountPath: /data
      name: data