build:
  # Мы добавили PORT=3000 перед установкой, чтобы Vite не падал
  command: export PORT=3000 && pnpm install && pnpm --filter @workspace/api-server build

run:
  command: pnpm --filter @workspace/api-server start
  persistence:
    - mountPath: /data
      name: data