{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "export PORT=3000 && npm install pnpm && npx pnpm install && npx pnpm --filter @workspace/api-server build",
    "start": "npx pnpm --filter @workspace/api-server start"
  },
  "dependencies": {
    "pnpm": "9.15.9"
  }
}