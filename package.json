{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build || true",
    "typecheck": "true"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@9.15.9"
}