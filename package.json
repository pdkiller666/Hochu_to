{
  "name": "workspace",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "npx pnpm --filter @workspace/api-server build",
    "start": "npx pnpm --filter @workspace/api-server start"
  },
  "dependencies": {
    "pnpm": "9.15.9"
  }
}